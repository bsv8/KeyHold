{
  "name": "@bsv8/keyhold",
  "version": "2.0.0",
  "license": "AGPL-3.0-or-later",
  "description": "KeyHold keymaster v2 format SDK",
  "type": "module",
  "exports": { ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" } },
  "files": ["dist", "README.md", "LICENSE"],
  "scripts": { "typecheck": "tsc -p tsconfig.json --noEmit", "build": "tsc -p tsconfig.build.json", "test": "vitest run", "fmt-check": "prettier --check 'src/**/*.ts' 'test/**/*.ts'" },
  "dependencies": { "@noble/curves": "^2.0.0" },
  "devDependencies": { "@types/node": "^22.0.0", "prettier": "^3.0.0", "typescript": "^5.7.0", "vitest": "^3.0.0" }
}
