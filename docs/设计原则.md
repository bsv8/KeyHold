设计原则：
1. 加密等复杂运算参数化，争取不要文档也可以解析这个 json
2. 参数化要做好标准化，不能随意关键字和数字单位
3. 每个 item 应该有 label
4. format 是 keymaster
5. 用不上的字段不要，不做过度设计
6. 格式没有默认参数；SDK 可以提供推荐参数，但调用方必须明确选择，最终实际参数必须完整写入 JSON
7. SDK 同时支持加密状态直接组装导出，以及输入明文私钥后完整加密导出；两种方式必须生成相同格式
