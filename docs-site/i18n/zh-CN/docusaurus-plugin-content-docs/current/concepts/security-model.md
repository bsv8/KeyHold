---
id: security-model
title: 安全模型
sidebar_label: 安全模型
---

# 安全模型

KeyHold 使用基于密码的加密和认证密文保护静态私钥。它不管理密码、轮换密钥、持久化文件，也不提供硬件支持的密钥存储。

安全边界保持有意的狭窄：密码和明文私钥由调用应用持有；解析在密码学操作前严格进行；只有 AES-GCM 认证成功后才返回明文；恢复出的公钥必须匹配 `publicKeyHex`；稳定错误码使调用方无需解析消息文本即可处理失败。

威胁建模、密码策略、内存清理、传输安全和访问控制仍由应用负责。
